create table images (
    job_id    varchar,
    seed      int,
    prompt    varchar,
    author_id varchar,
    model_id  integer
);

create table models (
    id          integer
        primary key autoincrement,
    name        varchar,
    endpoint_id varchar,
    aliases     varchar
);

create table user_settings (
    user_id  varchar,
    model_id integer,
    width    integer,
    height   integer
);