CREATE TABLE IF NOT EXISTS images (
    job_id          varchar,
    seed            int,
    parameters      varchar,
    author_id       varchar,
    model_id        integer
);

CREATE TABLE IF NOT EXISTS user_settings (
    user_id         varchar,
    model_id        integer,
    width           integer,
    height          integer,
    negative_prompt varchar
);